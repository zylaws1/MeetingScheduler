<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xinshen.comp2100_meetingschedule">

    <application
        android:allowBackup="true"
        android:name=".MeetingApplication"
        android:icon="@mipmap/meetings_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/meetings_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".main.MainActivity">
            <intent-filter android:order="1">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="xinshen.example.com"
                    android:pathPattern="/.*"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.SearchActivity">
        </activity>
        <activity
            android:name=".main.MeetingInfoActivity">
        </activity>
        <activity
            android:name=".main.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.FeedbackActivity">
        </activity>
        <activity
            android:name=".main.AboutActivity">
        </activity>

        <activity
            android:name=".main.QuickHelpActivity">
        </activity>

        <activity
            android:name=".ui.login.RegisterActivity">
        </activity>
    </application>

</manifest>